# Hash & HMAC Generator Tool

Computes standard cryptographic digests (SHA-256, SHA-512, SHA-1, MD5) and keyed HMAC signatures.

## Parameters
- `text` (required): Input string.
- `algorithm` (optional): `sha256`, `sha512`, `sha1`, `md5`.
- `key` (optional): Secret key for HMAC.
