# JSON <-> YAML Converter Tool

Bi-directional conversion utility between JSON and YAML formats with zero external dependencies.

## Parameters
- `content` (required): Input string in JSON format.
- `target_format` (optional): `'yaml'` (default) or `'json'`.
- `indent` (optional): Indentation spacing (default: `2`).
